---
services:
  serviio:
    build: .
    network_mode: "host"
    environment: 
      - IPADDR=192.168.1.2
    volumes:
      - /etc/localtime:/etc/localtime:ro
      - /mnt/media/data:/mnt/media
      - /home/andry/serviio-library:/root/serviio-1.7/library
      - /mnt/media/data/serviio-transcode:/mnt/transcode
    restart: unless-stopped
    command: [bash, ./serviio.sh]
version: "2"
