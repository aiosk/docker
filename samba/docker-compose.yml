---
services:
  samba:
    build: .
    network_mode: "host"
    volumes:
      - /d/Documents/Projects/docker/samba/data:/shares:rw
    restart: unless-stopped
    stdin_open: true
    command: sh -c "inetd -d & /usr/sbin/nmbd -FS -d 1 & /usr/sbin/smbd -FS -d 2"
version: "2"
